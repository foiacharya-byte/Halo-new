# Reference materials

This folder is for **narrative/storyboard references only** — material that
explains scroll behaviour, hierarchy and pacing, never material treated as
approved city imagery or a layout to copy verbatim (see the master prompt's
own caveat on the storyboard screenshot).

## `halo-scroll-story-reference.png` — not yet saved here

This session received the storyboard screenshot and the "ae halo" logo
image as pasted chat attachments, not as files on disk. There is currently
no tool available in this session that can write image bytes I've only
*seen* rendered in conversation out to the filesystem — so this file could
not actually be created from that paste, and it would be dishonest to
claim otherwise by leaving a fabricated placeholder here.

**To get the real files into the repo**, one of these will work:
- Commit them to this repo directly (e.g. via the GitHub web UI or a local
  `git add`/`git push`) at `public/assets/halo/reference/halo-scroll-story-reference.png`
  and `public/assets/halo/brand/ae-halo-logo.png` (or `.svg` if you have a
  vector source) — I can then read and use them normally.
- Provide a URL I can fetch from, if the files are hosted somewhere.

Once either file exists on disk, this README's job is done and can be
deleted.
